import { AppShell } from "@/components/layout/app-shell";

export default function SettingsPage() {
  return (
    <AppShell sectionTitle="Settings">
      <p>Settings scaffold for app and voice preferences.</p>
    </AppShell>
  );
}
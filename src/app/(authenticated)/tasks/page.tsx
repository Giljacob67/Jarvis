import { AppShell } from "@/components/layout/app-shell";

export default function TasksPage() {
  return (
    <AppShell sectionTitle="Tasks">
      <p>Tasks module scaffold (Phase 2+ implementation).</p>
    </AppShell>
  );
}
import { AppShell } from "@/components/layout/app-shell";

export default function CalendarPage() {
  return (
    <AppShell sectionTitle="Calendar">
      <p>Calendar module scaffold (Phase 2+ implementation).</p>
    </AppShell>
  );
}